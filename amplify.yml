version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g corepack@latest
        - corepack enable
        - corepack prepare pnpm@11.24.0 --activate
        - pnpm --version
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
